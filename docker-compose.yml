version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_OPTIONS=--openssl-legacy-provider
    command: ["yarn", "start"]
    volumes:
      - .:/app
      - /app/node_modules
